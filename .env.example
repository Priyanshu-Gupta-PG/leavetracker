# Example environment values for running locally
# Get a WalletConnect Project ID at https://cloud.walletconnect.com and replace below
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=YOUR_WALLETCONNECT_PROJECT_ID

# Optional: deployed contract address (update if you're pointing to a live/testnet instance)
# CONTRACT_ADDRESS=0x...
